# AeroShield IQ — Backend Requirements
# Install: pip install -r requirements.txt

# ── Web framework ─────────────────────────────────────────────────────────────
fastapi==0.111.0
uvicorn==0.30.1
pydantic==2.7.4
python-dotenv==1.0.1

# ── ML & Data ─────────────────────────────────────────────────────────────────
pandas==2.2.2
numpy==1.26.4
lightgbm==4.3.0
scikit-learn==1.5.0
joblib==1.4.2
scipy==1.13.0

# ── Geospatial ────────────────────────────────────────────────────────────────
geopandas==0.14.4
shapely==2.0.4

# ── LLM / Agent framework ─────────────────────────────────────────────────────
groq==0.9.0                  # kept for fallback compatibility
langchain-groq==0.1.9        # LangChain wrapper for Groq
langchain-core==0.2.10       # LangChain core primitives
langgraph==0.1.14            # Multi-agent graph orchestration

# ── Data fetching ─────────────────────────────────────────────────────────────
requests==2.32.3             # Open-Meteo API calls
tqdm==4.66.4                 # Download progress bars

# ── Optional: OpenAQ client for step1 downloader ──────────────────────────────
# openaq==2.1.0              # Uncomment if using step1_download_openaq.py